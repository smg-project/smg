name: 'Setup TokenSpeed Backend'
description: 'Create Python venv and install TokenSpeed (engine + kernel + scheduler) from source.'

inputs:
  github-token:
    description: >-
      Token used to fetch the TokenSpeed source from github.com. Only a public
      read is needed, so the caller's own GITHUB_TOKEN suffices. Optional: an
      empty value leaves the fetch anonymous.
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Setup Python venv
      shell: bash
      run: bash scripts/ci_setup_python_venv.sh

    - name: Install TokenSpeed
      shell: bash
      env:
        TOKENSPEED_GITHUB_TOKEN: ${{ inputs.github-token }}
      run: bash scripts/ci_install_tokenspeed.sh
