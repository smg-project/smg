name: Nightly MLX Benchmark

on:
  # Temporarily disabled — nightly schedule commented out. Re-enable by
  # uncommenting the cron below. Manual runs via workflow_dispatch still work.
  # schedule:
  #   # 09:17 UTC — off-peak, off the round hour.
  #   - cron: "17 9 * * *"
  workflow_dispatch:
    inputs:
      model:
        description: "Model id. Default: Gemma 3 4B QAT (~3 GB), known-working with the mlx-lm pin."
        required: false
        default: "mlx-community/gemma-3-4b-it-qat-4bit"
      phases:
        description: "Phases to run (space-separated subset of 'mlx grpc')."
        required: false
        default: "mlx grpc"
      scenarios:
        description: "Space-separated scenarios (chat agent)"
        required: false
        default: "chat agent"
      chat_concurrencies:
        description: "Space-separated concurrencies for the chat scenario"
        required: false
        default: "1 4"
      agent_concurrencies:
        description: "Space-separated concurrencies for the agent scenario"
        required: false
        default: "1 4"
      chat_requests:
        description: "Completed-request target per chat cell (runs are request-bounded)"
        required: false
        default: "60"
      agent_requests:
        description: "Completed-request target per agent cell (runs are request-bounded)"
        required: false
        default: "40"
      duration_min:
        description: "Per-cell wall-clock backstop (minutes); safety cap only, not the primary limit"
        required: false
        default: "30"

permissions:
  contents: read

concurrency:
  group: nightly-mlx-bench-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  bench:
    name: MLX bench (mlx-lm vs SMG+gRPC)
    runs-on: macos-latest
    # Request-bounded sweep: chat c1/c4 (60 reqs) + agent c1/c4 (40 reqs), both
    # backends. From observed RPS that's ~2.5-3 h of benchmarking plus build +
    # model download. The 30-min/cell backstop caps a stalled cell; cap the job
    # at 5 h for safety. (c16/c64 are excluded — see scripts/mlx_bench.sh.)
    timeout-minutes: 300
    env:
      MODEL: ${{ github.event.inputs.model || 'mlx-community/gemma-3-4b-it-qat-4bit' }}
      PHASES: >-
        ${{ github.event.inputs.phases || 'mlx grpc' }}
      SCENARIOS: >-
        ${{ github.event.inputs.scenarios || 'chat agent' }}
      CHAT_CONCURRENCIES: >-
        ${{ github.event.inputs.chat_concurrencies || '1 4' }}
      AGENT_CONCURRENCIES: >-
        ${{ github.event.inputs.agent_concurrencies || '1 4' }}
      CHAT_REQUESTS: >-
        ${{ github.event.inputs.chat_requests || '60' }}
      AGENT_REQUESTS: >-
        ${{ github.event.inputs.agent_requests || '40' }}
      DURATION_MIN: >-
        ${{ github.event.inputs.duration_min || '30' }}
      RESULTS_DIR: bench-results
      PYTHONUNBUFFERED: "1"
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.98.0

      - name: Install protoc
        run: brew install protobuf

      - name: Cache Cargo registry and target
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: mlx-bench
          cache-on-failure: true

      - name: Build smg (release)
        run: cargo build --release --bin smg

      - name: Build smg-grpc-proto Python package
        run: pip install -e ./crates/grpc_client/python

      - name: Install grpc_servicer with MLX extra
        run: pip install -e "./grpc_servicer[mlx]"

      - name: Install mlx-lm CLI (for the direct-HTTP baseline)
        # Pinned so nightly bench trends aren't shifted by upstream mlx-lm
        # changes independent of SMG. Bump deliberately in its own commit.
        run: pip install "mlx-lm==0.31.3"

      - name: Install genai-bench (driver)
        # `oci_openai` is a transitive dep genai-bench's CLI imports eagerly;
        # pin it explicitly so `genai-bench --help` doesn't crash on a clean install.
        run: pip install genai-bench oci_openai

      - name: Patch genai-bench streaming chunk size
        # mlx-lm.server uses stdlib BaseHTTPRequestHandler (HTTP/1.0, no
        # chunked encoding), so urllib3's read(amt=None) buffers to EOF
        # and collapses per-token timing into one bulk read. chunk_size=1
        # makes urllib3 yield per recv(), restoring real TTFT/TPOT.
        # No-op for the gRPC path (HTTP/1.1 + chunked).
        run: |
          target=$(python3 -c "import genai_bench, os; print(os.path.dirname(genai_bench.__file__) + '/user/openai_user.py')")
          echo "Patching $target"
          sed -i.bak 's/response.iter_lines(chunk_size=None)/response.iter_lines(chunk_size=1)/g' "$target"
          if ! grep -q "iter_lines(chunk_size=1)" "$target"; then
            echo "Patch did not apply — pattern not found" >&2
            exit 1
          fi

      - name: Verify components
        run: |
          ./target/release/smg --help | head -5
          python3 -c "from smg_grpc_servicer.mlx.server import main; print('servicer OK')"
          genai-bench --help | head -3

      - name: Run benchmark — Phase 1 (mlx-lm.server, direct HTTP)
        if: contains(env.PHASES, 'mlx')
        run: PHASES=mlx ./scripts/mlx_bench.sh

      - name: Run benchmark — Phase 2 (SMG router + MLX gRPC servicer)
        # Always run phase 2 even if phase 1 failed — phases are independent.
        if: always() && contains(env.PHASES, 'grpc')
        run: PHASES=grpc ./scripts/mlx_bench.sh

      - name: Aggregate results
        if: always()
        run: |
          python3 scripts/mlx_bench_aggregate.py \
            --results-dir "$RESULTS_DIR" \
            --model "$MODEL" \
            --out "$RESULTS_DIR/SUMMARY.md"
          cat "$RESULTS_DIR/SUMMARY.md" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload raw genai-bench output
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: nightly-mlx-bench-${{ github.run_id }}
          path: bench-results/
          retention-days: 30

      - name: Upload server logs on failure
        if: failure() || cancelled()
        uses: actions/upload-artifact@v7
        with:
          name: nightly-mlx-bench-logs-${{ github.run_id }}
          path: |
            bench-results/mlx-lm.log
            bench-results/mlx-grpc.log
            bench-results/smg-router.log
          retention-days: 14
          if-no-files-found: ignore
